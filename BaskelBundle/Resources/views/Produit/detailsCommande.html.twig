{% extends 'baseAdmin.html.twig' %}

{% block content %}


    <div class="content mt-3">
        <div class="animated fadeIn">
            <div class="row">

                <div class="col-md-12">
                    <div class="card">
                        <div class="card-header">
                            <strong class="card-title"> Détails de la Commande </strong>
                        </div>
                        <div class="card-body">
                            <table id="bootstrap-data-table-export" class="table table-striped table-bordered">
                                <thead>
                                <tr>

                                    <th>Image</th>
                                    <th>Nom</th>
                                    <th>Reference</th>
                                    <th>Quantite</th>
                                    <th>Prix Unitaire</th>
                                    <th>Total</th>

                                </tr>
                                </thead>
                                <tbody>
                                <tr>
                                    {% for panier in paniers %}
                                <tr>
                                    <td><img class="imageproduit2" src="{{asset('uploads/images/' ) }}{{ panier.idProduit.image }}"  width="110" height="110">  </td>
                                    <td>{{ panier.idProduit.nom }}</td>
                                    <td>{{ panier.idProduit.reference }}</td>
                                    <td>{{ panier.quantite }}</td>
                                    <td>{{ panier.idProduit.prix }}</td>
                                    <td>{{ panier.idProduit.prix * panier.quantite }}</td>
                                </tr>



                                {% endfor %}

                                </td>
                                </tr>

                            </table>
                        </div>
                    </div>
                </div>


            </div>
        </div>
    </div>




{% endblock %}
